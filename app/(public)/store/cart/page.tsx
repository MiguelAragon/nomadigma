'use client';

import { Container } from '@/components/ui/container';
import { CartContent } from '@/app/(public)/store/cart/content';

export default function CartPage() {
  return (
    <Container>
      <CartContent />
    </Container>
  );
} 